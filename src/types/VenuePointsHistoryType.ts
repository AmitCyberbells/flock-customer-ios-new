type VenuePointsHistoryType = {
    venue_id: number,
    venue_name: string,
    venue_image?: string,
    venue_location?: string,
    earned_feather_points: number,
    spent_feather_points: number,
    earned_venue_points: number,
    spent_venue_points: number,
}

export default VenuePointsHistoryType;
