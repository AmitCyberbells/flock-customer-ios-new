const RedeemBy = {
    feather_points: 'feather_points',
    venue_points: 'venue_points'
}

type OfferRedeemBy = 'feather_points' | 'venue_points';

export default OfferRedeemBy;

export { RedeemBy };